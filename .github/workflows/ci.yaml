name: contionuos integration
on: [workflow_dispatch]
jobs:
  docker:
    uses: RoboShop1/github-reusable/.github/workflows/docker-ci@main
    with:
      component_name: ${{ github.repository }}
    secrets: inherit

